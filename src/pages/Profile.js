import React, { Component } from 'react';
// import PropTypes from 'prop-types';
import { Header } from '../components';

class Profile extends Component {
  render() {
    return (
      <div>
        <Header />
        Eu sou página de Profile
      </div>
    );
  }
}

// Drinks.propTypes = {

// };

export default Profile;
