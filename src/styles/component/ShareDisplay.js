import styled from 'styled-components';

const StyledSharedDisplay = styled.span`
  display: flex;
  align-items: center;

  button {
    background-color: initial;
    border: none;
    margin: 5px;

    img {
      width: 30px;
    }
  }
`;

export default StyledSharedDisplay;
